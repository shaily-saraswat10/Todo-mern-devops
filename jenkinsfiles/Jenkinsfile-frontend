pipeline {
    agent any

    stages {
        stage('Cleaning Workspace') {
            steps {
                cleanWs()
            }
        }
        stage('Git clone') {
            steps {
                git branch: 'main', url: 'https://github.com/shaily-saraswat10/Todo-mern-devops.git'
            }
        }
        stage('Docker image build') {
            steps {
                dir('client') {
                    sh 'docker build -t todo-frontend:01 .'
                }
            }
        }
        stage('Docker image push') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerHubCred',
                    usernameVariable: 'dockerHubUser',
                    passwordVariable: 'dockerHubPass')]) {
                        sh 'docker tag todo-frontend:01 $dockerHubUser/todo-frontend:01'
                        sh 'docker login -u $dockerHubUser -p $dockerHubPass'
                        sh 'docker push $dockerHubUser/todo-frontend:01'
                    }
            }
        }
    }
}
